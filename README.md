# pubmed-rag

My goal with this project is to learn how to implement a RAG system using a HuggingFace embeddings + a vector database, over a subset of PubMed abstracts for specific disease areas. A user can ask a clinical question, and the system retrieves relevant papers and generates a grounded answer.