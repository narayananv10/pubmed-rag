# Streamlit Cloud secrets. Paste these in the Streamlit Cloud UI under
# Settings → Secrets when deploying. For local dev use .env instead — this
# file is only an example for the deployed app.

VOYAGE_API_KEY = ""
ANTHROPIC_API_KEY = ""
